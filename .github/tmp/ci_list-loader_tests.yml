# name: Run list-loader tests

# on:
#   push:
#     branches:
#     - '**'
#     tags-ignore:
#     - '**'
#     paths:
#     - 'projects/list-loader/**'
#   pull_request:
#   workflow_call:
#   workflow_dispatch:

# concurrency:
#   group: run-list-loader-tests-group-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   ci_list-loader_tests:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout sources
#         uses: actions/checkout@v4

#       - name: Run tests
#         uses: ./.github/actions/run-tests
#         with:
#           working-directory: projects/list-loader
