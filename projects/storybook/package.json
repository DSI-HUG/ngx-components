{
    "private": true,
    "scripts": {
        "start": "ng serve storybook",
        "lint": "eslint . --fix",
        "test": "ng test storybook",
        "test:ci": "ng test storybook --watch=false --browsers=ChromeHeadless",
        "build": "ng build storybook -c production"
    }
}
